services:
  migrations:
    image: ${MIGRATIONS_IMAGE:?MIGRATIONS_IMAGE must be set}
    env_file:
      - ${ENV_FILE:-.env.production}
    networks:
      - database

networks:
  database:
    external: true
    name: lifeos-database
